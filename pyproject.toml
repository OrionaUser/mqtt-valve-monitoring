[project]
name = "mqtt-valve-monitoring"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "karthiknOriona", email = "karthik@orionaglobal.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "paho-mqtt>=2.1.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
mqtt-valve-monitoring = "mqtt_valve_monitoring:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
